## Experiment Title
Experiment 3

## Aim
To write a Python program to perform basic string operations.

## Algorithm

## Algorithm

1. Initialize a string variable named `line`.
2. Print the string in lowercase using the `lower()` method.
3. Print the string in uppercase using the `upper()` method.
4. Convert the string to title case using `title()` and display it.
5. Swap the uppercase and lowercase characters using `swapcase()`.
6. Capitalize the first character of the string using `capitalize()`.
7. Find and display the length of the string using `len()`.
8. Split the string into words using `split()` and print the list.
9. Replace `"sing"` with `"draw"` using the `replace()` method and display the new string.
10. Find and print the index position of `"like"` using the `index()` method.

## Output

```text
Lowercase: i'm sharuni, i like to sing
Uppercase: I'M SHARUNI, I LIKE TO SING
Title Case: I'M Sharuni, I Like To Sing
Swapcase: i'M SHARUNI, i LIKE TO SING
Capitalize: I'm sharuni, i like to sing
Length: 27
Split: ["I'm", 'sharuni,', 'I', 'like', 'to', 'sing']
Replace: I'm sharuni, I like to draw
Index of 'like': 15
```